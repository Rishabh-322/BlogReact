apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: blogapp

spec:

  selector:
    app: frontend

  ports:

    - name: http
      port: 8080
      targetPort: 8080

  type: ClusterIP